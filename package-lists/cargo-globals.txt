# Cargo global installs
# Install: cargo install <package>
# For versioned installs: cargo install <package> --version <version>

# ── Solana / Anchor / Arcium ───────────────────────────────────
# Arcium requires EXACT versions — do not upgrade without checking docs first
# Docs: https://docs.arcium.com/developers/installation
#
# 1. Install avm (Anchor Version Manager)
#    cargo install avm
# 2. Install and pin Anchor 1.0.2
#    avm install 1.0.2 && avm use 1.0.2
# 3. Install Solana CLI 3.1.10
#    sh -c "$(curl -sSfL https://release.anza.xyz/v3.1.10/install)"
# 4. Install arcup (Arcium CLI) via install_arcium.sh in scripts/
#
avm                             # Anchor Version Manager — then: avm install 1.0.2 && avm use 1.0.2

# ── ZK / Circuits ──────────────────────────────────────────────
# circom installed from local source — build from: https://github.com/iden3/circom
# git clone https://github.com/iden3/circom && cargo install --path circom

# ── Cargo dev tools ────────────────────────────────────────────
cargo-nextest                   # Fast parallel test runner (replaces cargo test)
cargo-llvm-cov                  # Code coverage via LLVM instrumentation
cargo-tarpaulin                 # Code coverage (alternative to llvm-cov)
cargo-deny                      # Dependency audit (licenses, advisories, bans)
cargo-deb                       # Build .deb packages from Cargo projects
cargo-cache                     # Manage and clean ~/.cargo/registry cache

# ── Build / WASM ───────────────────────────────────────────────
wasm-pack                       # Build Rust → WebAssembly packages
tree-sitter-cli                 # Build and test Tree-sitter grammars

# ── Database ───────────────────────────────────────────────────
sqlx-cli                        # SQLx database migrations CLI

# ── Learning / Misc ────────────────────────────────────────────
rustlings                       # Interactive Rust exercises
honggfuzz                       # Fuzzing (cargo-hfuzz / cargo-honggfuzz)
